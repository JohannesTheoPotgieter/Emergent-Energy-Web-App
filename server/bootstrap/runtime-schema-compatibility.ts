import { sql } from "drizzle-orm";
import { db } from "../db";

export async function runRuntimeSchemaCompatibility(
  enabled: boolean,
  log: (message: string, source?: string) => void,
) {
  if (!enabled) return;

  // Transitional runtime compatibility only. Permanent schema evolution should be moved to migrations.
  const blocks: Array<{ name: string; statement: string }> = [
    { name: "soft-delete-columns", statement: `ALTER TABLE operational_tasks ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMP; ALTER TABLE mytool_tasks ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMP;` },
    { name: "ee-info-columns", statement: `ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS primary_instruction TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS stage_code TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS definition_of_done TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS owner_role_id TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS approver_role_id TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS required_links JSONB; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS example_artifacts JSONB; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS example_notes TEXT; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS common_pitfalls JSONB; ALTER TABLE ee_info_nodes ADD COLUMN IF NOT EXISTS next_node_id TEXT;` },
    { name: "pm-on-the-go", statement: `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'pm_action_type') THEN CREATE TYPE pm_action_type AS ENUM ('site_visit','generate_po','link_invoice','raise_variation','log_delay','log_risk','upload_photo','update_progress','escalate'); END IF; IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'pm_action_status') THEN CREATE TYPE pm_action_status AS ENUM ('pending','approved','rejected','completed'); END IF; IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'pm_safety_status') THEN CREATE TYPE pm_safety_status AS ENUM ('clear','issue_open'); END IF; END $$; CREATE TABLE IF NOT EXISTS pm_site_visits (id SERIAL PRIMARY KEY, project_id INTEGER NOT NULL REFERENCES project_info(id), user_id INTEGER NOT NULL REFERENCES users(id), visit_date DATE NOT NULL, notes TEXT, weather_conditions TEXT, safety_status pm_safety_status DEFAULT 'clear', photo_ids JSONB DEFAULT '[]', created_at TIMESTAMP DEFAULT NOW(), created_by TEXT, updated_at TIMESTAMP DEFAULT NOW(), updated_by TEXT, source TEXT DEFAULT 'on_the_go'); CREATE TABLE IF NOT EXISTS pm_on_the_go_actions (id SERIAL PRIMARY KEY, project_id INTEGER NOT NULL REFERENCES project_info(id), user_id INTEGER NOT NULL REFERENCES users(id), action_type pm_action_type NOT NULL, title TEXT NOT NULL, description TEXT, severity TEXT, amount DECIMAL(15,2), status pm_action_status DEFAULT 'pending', related_entity_id INTEGER, related_entity_type TEXT, metadata JSONB, created_at TIMESTAMP DEFAULT NOW(), created_by TEXT, updated_at TIMESTAMP DEFAULT NOW(), updated_by TEXT, source TEXT DEFAULT 'on_the_go'); CREATE TABLE IF NOT EXISTS pm_compliance_tracking (id SERIAL PRIMARY KEY, project_id INTEGER NOT NULL, user_id INTEGER NOT NULL, week_start_date DATE NOT NULL, daily_diary_done JSONB DEFAULT '[]', weekly_progress_done BOOLEAN DEFAULT FALSE, weekly_risk_done BOOLEAN DEFAULT FALSE, created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW(), UNIQUE(project_id, user_id, week_start_date)); CREATE TABLE IF NOT EXISTS pm_mode_preferences (id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL UNIQUE REFERENCES users(id), preferred_mode TEXT DEFAULT 'full_detail', updated_at TIMESTAMP DEFAULT NOW());` },
    { name: "ms-objects", statement: `ALTER TABLE ms_objects ADD COLUMN IF NOT EXISTS dismissed BOOLEAN DEFAULT FALSE;` },
    { name: "portfolio", statement: `CREATE TABLE IF NOT EXISTS portfolios (id SERIAL PRIMARY KEY, name TEXT NOT NULL, client_name TEXT, status TEXT NOT NULL DEFAULT 'Active', description TEXT, owner_user_id INTEGER REFERENCES users(id), created_by INTEGER REFERENCES users(id), updated_by INTEGER REFERENCES users(id), created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS portfolio_rollout_plans (id SERIAL PRIMARY KEY, portfolio_id INTEGER NOT NULL REFERENCES portfolios(id) ON DELETE CASCADE, name TEXT NOT NULL, notes TEXT, created_by INTEGER REFERENCES users(id), updated_by INTEGER REFERENCES users(id), created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS portfolio_rollout_phases (id SERIAL PRIMARY KEY, rollout_plan_id INTEGER NOT NULL REFERENCES portfolio_rollout_plans(id) ON DELETE CASCADE, phase_name TEXT NOT NULL, start_date TEXT, end_date TEXT, target_kwp DECIMAL(12,2), target_revenue DECIMAL(15,2), sort_order INTEGER NOT NULL DEFAULT 0); CREATE TABLE IF NOT EXISTS project_portfolio_assignments (id SERIAL PRIMARY KEY, project_id INTEGER NOT NULL REFERENCES project_info(id) UNIQUE, portfolio_id INTEGER NOT NULL REFERENCES portfolios(id) ON DELETE CASCADE, assigned_by INTEGER REFERENCES users(id), assigned_at TIMESTAMP NOT NULL DEFAULT NOW(), moved_by INTEGER REFERENCES users(id), moved_at TIMESTAMP);` },
    { name: "work-items-and-client-schema", statement: `ALTER TABLE project_info ADD COLUMN IF NOT EXISTS client_id INTEGER REFERENCES clients(id); CREATE INDEX IF NOT EXISTS idx_project_info_client_id ON project_info(client_id); DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'work_item_workstream') THEN CREATE TYPE work_item_workstream AS ENUM ('PD', 'ENG', 'QUALITY', 'PM', 'FINANCE', 'PERSONAL', 'GOVERNANCE'); END IF; IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'work_item_source') THEN CREATE TYPE work_item_source AS ENUM ('SMART_IMPORT', 'UI', 'INTEGRATION', 'SYSTEM'); END IF; IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'work_item_assignment_role') THEN CREATE TYPE work_item_assignment_role AS ENUM ('OWNER', 'ASSIGNEE', 'REVIEWER', 'VIEWER'); END IF; IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'work_item_dep_type') THEN CREATE TYPE work_item_dep_type AS ENUM ('FS', 'SS', 'FF', 'SF'); END IF; END $$; CREATE TABLE IF NOT EXISTS work_items (id SERIAL PRIMARY KEY, client_id INTEGER REFERENCES clients(id), project_id INTEGER REFERENCES project_info(id), workstream work_item_workstream NOT NULL, type TEXT, source work_item_source NOT NULL DEFAULT 'UI', title TEXT NOT NULL, description TEXT, status TEXT NOT NULL DEFAULT 'Not Started', priority TEXT, start_date TEXT, end_date TEXT, duration INTEGER, percent_complete REAL DEFAULT 0, wbs_code TEXT, outline_number TEXT, parent_id INTEGER, owner_user_id INTEGER REFERENCES users(id), is_shared BOOLEAN NOT NULL DEFAULT FALSE, external_ref TEXT UNIQUE, legacy_table TEXT, legacy_id INTEGER, created_by INTEGER REFERENCES users(id), created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW(), deleted_at TIMESTAMP); CREATE TABLE IF NOT EXISTS work_item_assignments (id SERIAL PRIMARY KEY, work_item_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, user_id INTEGER NOT NULL REFERENCES users(id), role work_item_assignment_role NOT NULL DEFAULT 'ASSIGNEE', allocation_pct REAL, created_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS work_item_dependencies (id SERIAL PRIMARY KEY, predecessor_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, successor_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, dep_type work_item_dep_type NOT NULL DEFAULT 'FS', lag_days INTEGER DEFAULT 0); CREATE TABLE IF NOT EXISTS work_item_comments (id SERIAL PRIMARY KEY, work_item_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, user_id INTEGER NOT NULL REFERENCES users(id), content TEXT NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS work_item_attachments (id SERIAL PRIMARY KEY, work_item_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, file_name TEXT NOT NULL, file_url TEXT NOT NULL, uploaded_by INTEGER REFERENCES users(id), created_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS work_item_status_history (id SERIAL PRIMARY KEY, work_item_id INTEGER NOT NULL REFERENCES work_items(id) ON DELETE CASCADE, old_status TEXT, new_status TEXT NOT NULL, changed_by INTEGER REFERENCES users(id), changed_at TIMESTAMP NOT NULL DEFAULT NOW(), reason TEXT); CREATE TABLE IF NOT EXISTS project_client_history (id SERIAL PRIMARY KEY, project_id INTEGER NOT NULL REFERENCES project_info(id), old_client_id INTEGER REFERENCES clients(id), new_client_id INTEGER REFERENCES clients(id), moved_by_user_id INTEGER NOT NULL REFERENCES users(id), moved_at TIMESTAMP NOT NULL DEFAULT NOW(), reason TEXT); CREATE TABLE IF NOT EXISTS import_field_mappings (id SERIAL PRIMARY KEY, import_profile TEXT NOT NULL, sheet_name TEXT, excel_column_header TEXT NOT NULL, target_table TEXT NOT NULL, target_field TEXT NOT NULL, transform TEXT, required BOOLEAN NOT NULL DEFAULT FALSE, default_value TEXT, active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS migration_backups (id SERIAL PRIMARY KEY, backup_id TEXT NOT NULL, backup_type TEXT NOT NULL DEFAULT 'manual', description TEXT, created_by_user_id INTEGER REFERENCES users(id), created_by_name TEXT, created_at TIMESTAMP NOT NULL DEFAULT NOW()); CREATE TABLE IF NOT EXISTS migration_cleanup_log (id SERIAL PRIMARY KEY, action TEXT NOT NULL, table_name TEXT NOT NULL, original_name TEXT NOT NULL, archived_name TEXT, row_count INTEGER, performed_by_user_id INTEGER REFERENCES users(id), performed_by_name TEXT, backup_id TEXT, reversible BOOLEAN NOT NULL DEFAULT TRUE, performed_at TIMESTAMP NOT NULL DEFAULT NOW(), metadata TEXT);` },
    { name: "work-items-columns", statement: `ALTER TABLE work_items ADD COLUMN IF NOT EXISTS expected_pct_complete REAL; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS indent_level INTEGER DEFAULT 0; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS is_milestone BOOLEAN DEFAULT FALSE; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS phase TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS owner_name TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS source_row INTEGER; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS source_sheet TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS import_run_id INTEGER; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS baseline_start TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS baseline_end TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS baseline_duration INTEGER; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS task_mode TEXT DEFAULT 'auto'; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS actual_start TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS actual_end TEXT; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS actual_duration INTEGER; ALTER TABLE work_items ADD COLUMN IF NOT EXISTS sort_order INTEGER DEFAULT 0;` },
    { name: "user-project-folders", statement: `CREATE TABLE IF NOT EXISTS user_project_folders (id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL REFERENCES users(id), project_name TEXT NOT NULL, folder_name TEXT NOT NULL, folder_path TEXT, updated_at TIMESTAMP NOT NULL DEFAULT NOW(), UNIQUE(user_id, project_name));` },
    { name: "smart-import-columns", statement: `ALTER TABLE smart_import_runs ADD COLUMN IF NOT EXISTS records_attempted INTEGER; ALTER TABLE smart_import_runs ADD COLUMN IF NOT EXISTS records_succeeded INTEGER; ALTER TABLE smart_import_runs ADD COLUMN IF NOT EXISTS records_failed INTEGER; ALTER TABLE smart_import_runs ADD COLUMN IF NOT EXISTS import_type TEXT;` },
    { name: "approvals-enhancements", statement: `ALTER TABLE approvals ADD COLUMN IF NOT EXISTS related_entity_type TEXT; ALTER TABLE approvals ADD COLUMN IF NOT EXISTS related_entity_id INTEGER; ALTER TABLE approvals ADD COLUMN IF NOT EXISTS assigned_approver INTEGER REFERENCES users(id); ALTER TABLE approvals ADD COLUMN IF NOT EXISTS due_date TIMESTAMP; ALTER TABLE approvals ADD COLUMN IF NOT EXISTS project_id INTEGER REFERENCES project_info(id); ALTER TABLE approvals ADD COLUMN IF NOT EXISTS approval_category TEXT;` },
    { name: "ms-account-sso-columns", statement: `ALTER TABLE ms_accounts ADD COLUMN IF NOT EXISTS sso_access_token TEXT; ALTER TABLE ms_accounts ADD COLUMN IF NOT EXISTS sso_token_expires_at TIMESTAMP;` },
  ];

  for (const block of blocks) {
    try {
      await db.execute(sql.raw(block.statement));
      log(`compatibility block applied: ${block.name}`, "Startup:Maintenance");
    } catch (error: any) {
      log(`compatibility block failed: ${block.name} (${error?.message || error})`, "Startup:Maintenance");
    }
  }
}
