/**
 * Shared Microsoft integration constants.
 * Import from here to avoid hardcoding values across multiple files.
 */

/** IANA timezone used for all Microsoft Graph API requests and event payloads. */
export const MS_TIMEZONE = "Africa/Johannesburg";
