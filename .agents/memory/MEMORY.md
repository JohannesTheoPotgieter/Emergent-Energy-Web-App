- [Project deletion spine](project-data-deletion.md) — `project_info` is the real project spine (not the empty `projects` table); name-search alone undercounts linked rows.
- [Import tool period-lock removed](import-period-lock-removed.md) — Smart Import commit paths intentionally NO LONGER enforce COS period locks (owner 2026-06-18); don't "restore" the guard — other finance lock paths stay.
- [Screen gating is default-OPEN](screen-gating-failsafe.md) — screens are visible unless an admin explicitly disables one (an `isEnabled=false` row); the real prod lockdown is **live-ready mode** (`shared/config/enabled-modules.ts`). A 2026-06-18 "default-HIDDEN until signed off" experiment broke prod (nothing backfilled sign-offs → all finance/settings screens 404'd) and was reverted by owner. Do NOT reintroduce default-hidden.
- [Finance Home revenue source](finance-home-revenue-source.md) — revenue KPI+chart read `/api/revenue-tracker` (ties to Revenue screen, has QB bar); COS/GP stay on `/api/finance/lines`; fail loud on tracker error.
