## KPI Traceability Audit — 2026-04-23

| KPI Name | Source Table(s) | Repository Method | API Route | Client Component | Snapshot Guard Present? | Issues |
|---|---|---|---|---|---|---|
| Total Planned Revenue (`revenue_planned`) | `project_revenue_summary` | N/A (no active route found for this KPI endpoint) | **MISSING ROUTE** (`/api/revenue-summary` not registered) | **MISSING UI** (no client fetch from `/api/revenue-summary`) | Unknown (route missing; cannot verify live query) | MISSING ROUTE; MISSING UI |
| Engineering Progress % (`eng_progress_pct`) | `project_eng_stages` | None (inline SQL in route) | `/api/portfolio-dashboard` exists; `/api/engineering-standup` **missing** | `client/src/pages/portfolios.tsx` (uses `engSummary` from `/api/portfolio-dashboard`) | N/A (table not treated as snapshot here; no `effective_to` predicate used) | Endpoint mismatch in KPI definition (`/api/engineering-standup` not found) |
| Quality Pass Rate % (`quality_pass_rate`) | `qc_item_instance` | None (inline SQL in route) | `/api/quality/checklists` and `/api/portfolio-dashboard` | `client/src/pages/qm-dashboard.tsx`, `client/src/pages/portfolios.tsx` | N/A for `qc_item_instance` in reviewed queries | None observed |
| Average Project Progress % (`project_avg_progress`) | `work_items` (PM SMART_IMPORT rows) via project-plan mapping | `storage.getProjectPlansByProject()` | `/api/project-plan/:projectName` and `/api/portfolio-dashboard` | `client/src/pages/projects.tsx`, `client/src/pages/portfolios.tsx` | N/A (`work_items` uses `deleted_at IS NULL` guard, not snapshot `effective_to`) | None observed |
| COS Realised (Tracker) (`cos_tracker_realised`) | `normalized_cost_lines` (canonical read path replacing legacy `program_expense`) | None (inline `db.select` in `/api/cos-tracker`) | `/api/cos-tracker` | `client/src/pages/cos.tsx`, `client/src/components/tabs/MonthlyRealisationTab.tsx` | **Yes** (`isNull(normalizedCostLines.effectiveTo)` present) | Source table in KPI definition is stale (`program_expense` vs canonical `normalized_cost_lines`) |
| GP Actual (Tracker) (`gp_tracker_actual`) | `normalized_cost_lines` + `normalized_revenue_lines` (via adapters) | `getHighRiskAllCostReadRows()` + `storage.getAllRevenueLinesForCashflow()` | `/api/gp-tracker` | `client/src/components/tabs/GpTrackerTab.tsx` | **Yes** (cost read service returns current lines; inflows repository includes `isNull(effectiveTo)`) | Source table names in KPI definition are stale (`program_expense/program_inflows` legacy wording) |
| Revenue Allocated (Tracker) (`revenue_tracker_allocated`) | `normalized_cost_lines` + `normalized_revenue_lines` (allocation from `revenueRecognitionAmount`) | `getHighRiskAllCostReadRows()` + `storage.getAllRevenueLinesForCashflow()` | `/api/revenue-tracker` | `client/src/pages/revenue-tracker.tsx`, `client/src/components/tabs/RevenueTrackerTab.tsx` | **Yes** (same canonical guarded readers as GP tracker) | Source table names in KPI definition are stale (`program_expense/program_inflows` legacy wording) |
| Dashboard Plan GP Margin % (`dashboard_plan_gp_margin`) | `normalized_revenue_lines`, `normalized_cost_lines` | None (inline reads + `storage.getAllProjectInfo()`) | `/api/program-dashboard` | `client/src/pages/dashboard.tsx` | **Yes** (`isNull(...effectiveTo)` on both normalized tables) | None observed |

### Additional diagnostic note (from requested `server/analytics-routes.ts` review)
- `/api/analytics/trends`, `/api/analytics/budget-waterfall`, and `/api/analytics/velocity` currently synthesize values in memory (placeholder-style), not from canonical repositories/tables. Not part of the KPI set above, but worth tracking separately.
