Use the current repo as source of truth. Build a production-ready “SSEG submissions” screen under Project Delivery.

Read first, before changing anything:
- CLAUDE.md
- client/src/config/app-navigation.ts
- client/src/config/page-registry.ts
- client/src/config/navigation-permissions.ts
- client/src/hooks/use-permissions.ts
- client/src/App.tsx
- client/src/pages/admin-settings/roles/role-permissions-matrix.tsx
- server/api/v2/policies/permission-catalog.ts
- server/permission-middleware.ts
- any existing SSEG/compliance/handover routes, repos, schema, and legacy /sseg handling

Goal:
Create a real operational page that visually matches the attached reference closely, but uses our actual design system, routing, permission model, and data.

Requirements:
1. Add a canonical Project Delivery route for “SSEG submissions”.
2. Add it to Project Delivery secondary nav, active state, and breadcrumbs.
3. Build the page with:
   - title + subtitle
   - KPI cards: Under review, Approved 30D, CoC pending, Rejections YTD
   - active submissions table: Project, Municipality, NRS Type, Status, Submitted, Decision
   - status chips
   - Start submission CTA
4. CTA must only render for users with edit/create rights. No dead buttons.
5. Reuse existing data and business logic first. Do not duplicate SSEG logic across HSE, handover, engineering, or quality.
6. If needed, create a dedicated permission entity such as `sseg_submissions` and wire it through:
   - page registry
   - navigation permission model
   - admin roles matrix
   - backend enforcement
   - default role catalog
7. VIEW controls nav visibility. EDIT controls create/update actions.
8. Follow repo conventions:
   - new server routes go in `server/routes/*.routes.ts`
   - route handlers must use repository layer, not direct db queries
   - use existing shared schema/types
   - no mock data if real data exists
9. Preserve existing business rules and redirects. If legacy /sseg exists, handle it safely.
10. Do not invent workflow states, approval logic, or municipality rules.

UI expectations:
- clean, modern, calm
- consistent with current Emergent styling
- strong hierarchy
- useful empty/loading/error states
- readable table and filters if already supported by data
- desktop-first, responsive enough for laptop

Deliverables after implementation:
- summary of what was built
- exact files changed
- route added
- permission entity added/used
- roles that can view vs edit
- data source used
- assumptions/gaps
- QA evidence

Mandatory QA:
- route renders
- nav item appears only for roles with view access
- page hides when view access removed
- Start submission hides when edit removed
- breadcrumbs work
- active secondary nav works
- empty/loading/populated states work
- no broken legacy SSEG links
- no TypeScript regressions