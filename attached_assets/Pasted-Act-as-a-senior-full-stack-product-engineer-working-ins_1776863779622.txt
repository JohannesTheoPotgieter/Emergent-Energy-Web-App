Act as a senior full-stack product engineer working inside our existing repo. Before changing anything, inspect the live codebase and GitHub history for the project detail page, stage gate view, and timeline component. Do not guess. Verify current structure first.

Objective:
Move the project timeline UI so it sits directly inside or immediately below the stage gate view on the project detail page, because stage progression and timeline events are part of the same user workflow. The current separation is poor UX.

Context from screenshot:
- Project detail page
- Stage gate section is visible higher up
- Timeline section currently sits too low and disconnected
- We want timeline to be visually and functionally tied to the gate/stage section

Your job:
1. Find the exact components, routes, data queries, and layout containers involved.
2. Review current implementation against the repo/GitHub state before editing.
3. Move the timeline component upward into the stage gate area without breaking:
   - stage gate logic
   - project detail layout
   - filters
   - role visibility
   - data loading
   - mobile/responsive behavior
4. Remove duplicated spacing, duplicated wrappers, or dead layout containers caused by the move.
5. Ensure the timeline still uses the same project context and does not create duplicate fetches unless required.
6. If the timeline is currently generic, make sure it remains scoped correctly to the current project and current gate/stage context where relevant.
7. Keep the UI clean and professional. It must look intentional, not like a component was dragged and dropped in a panic.

Required review before code changes:
- Identify frontend files used for:
  - project detail page
  - stage gate card/view
  - timeline component
  - timeline filters
- Identify backend/API sources used by the timeline
- Confirm whether permissions affect timeline visibility or stage gate visibility
- Check whether any tests already cover this area
- Review recent GitHub commits touching these files so you do not overwrite working logic

Business rules to preserve:
- Do not change gate logic, workflow status logic, or approval logic unless strictly needed
- Do not break project delivery workflow
- Do not change permissions unless there is a mismatch that must be fixed
- Do not introduce duplicate capture or duplicate data logic
- Frontend and backend must stay aligned
- Preserve existing project-as-spine logic

Implementation requirements:
- Timeline should appear in the stage gate section in a way that makes workflow sense
- Preferred order:
  1. Stage gate header / status
  2. Gate progress / gate actions
  3. Timeline tabs or timeline view
  4. Timeline filters
  5. Timeline content
- If there is a better layout after reviewing the actual code, use it, but explain why
- Keep spacing tight and hierarchy clear
- Make sure empty states still render properly
- Make sure loading and error states still work
- Make sure no dead-end buttons or tabs remain after the move

Technical expectations:
- Reuse existing components where possible
- Do not duplicate timeline code
- Refactor only where needed to improve maintainability
- If current component architecture is messy, separate quick fix vs proper long-term cleanup
- Avoid hidden regressions
- Check for CSS/layout side effects on desktop and smaller screens

Validation:
After implementation, test and confirm:
- timeline renders in the new location
- stage gate still works
- timeline filters still work
- no console errors
- no duplicate API calls or obvious performance regression
- permissions still behave correctly for different roles
- no broken layout in project delivery pages

Output format required:
1. Current-state findings
2. Root cause / layout cause
3. Files changed
4. Exact fix implemented
5. Risks found
6. Anything not changed and why
7. QA checklist with pass/fail
8. Suggested follow-up improvements if needed

Important:
Do not just move markup blindly. Understand the workflow first, then implement the cleanest solution.